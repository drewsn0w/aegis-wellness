# Email settings for quote requests (SMTP)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=

# Where quote requests are delivered
QUOTE_TO_EMAIL=

# Used in the email subject/body
SITE_NAME=Aegis Wellness

# Optional: override the email sender address
# SMTP_FROM=
